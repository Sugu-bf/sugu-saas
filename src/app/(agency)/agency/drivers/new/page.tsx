import { CreateDriverForm } from "./create-driver-form";

export const metadata = {
  title: "Ajouter un livreur — Agence SUGU",
};

export default function CreateDriverPage() {
  return <CreateDriverForm />;
}
