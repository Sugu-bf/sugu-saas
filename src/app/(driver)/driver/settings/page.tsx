import { SettingsPageClient } from "./settings-content";

export const metadata = { title: "Paramètres — Coursier SUGU" };

export default function DriverSettingsPage() {
  return <SettingsPageClient />;
}
